# 構成

![hashidate の構成](../images/architecture-ja.svg)

プロセスが 3 つとページが 1 つです。呼び出し側が制御サーバにコマンドを投げ、サーバが SSE でレンダラへ流し、レンダラが結果を報告します。OBS はレンダラのページを写します。すべて `127.0.0.1` に bind します。

## リポジトリの構成

| パス | 中身 |
|---|---|
| `src/engine` | ランタイム本体。プロファイル、リグ、可動域、モーション、表情、揺れもの、ディレクタ、セッション。three.js には依存しますが、ブラウザの API には依存しません。 |
| `src/avatars` | モデル 1 体につき記述子 1 ファイル。アバターを増やすとはファイルを増やすことです。 |
| `src/protocol` | 通信仕様を zod スキーマで定義したもの。ビューア・サーバ・CLI がこれを共有するので、コマンド語彙が三者でずれません。 |
| `src/viewer` | レンダラ。three.js のステージと、その横に開発用のコンソール。OBS が向くのはこのページです。 |
| `src/panel` | 配信パネル（`/panel/`）。ここでの操作はすべて制御 API を通るので、パネルにできることはオーケストレータにもできます。 |
| `src/monitor` | ステージ（`/monitor/`）。同じレンダラを 16:9 のレターボックスに収めただけのページで、オペレータが見て、OBS が音を拾うのはここです。 |
| `src/shell` | ネイティブの起動役。2 つのページをウィンドウとして開き、サーバとサイドカーを下で立ち上げます。開発用チェックアウトのための便宜であって、意図的にパッケージ化していません。 |
| `src/server` | ローカル制御 API。3 つのページの配信と、コマンドの中継を兼ねます。 |
| `src/control` | その API を呼ぶ Node 側のクライアント。`ctl` と MCP アダプタが共有します。 |
| `src/cli` | `ctl`。手で動かすための薄いクライアントです。 |
| `src/mcp` | MCP アダプタ。同じ制御 API を、言語モデルに渡せるツールの形で話します。 |
| `src/script` | 台本——あらかじめ書いておいたターンの並び——を読む部分。語彙は増やしません。1 行がターンそのもので、setup の 1 項目がコマンドそのものです。 |
| `tools/blender` | モデル変換。Blender 組み込みの Python で動くため、ここだけ Python です。 |
| `tools/tts` | 音声サイドカー。音声モデルが PyTorch のコードベースなので、ここも Python です。HTTP 越しに呼ぶだけで import はしません。 |
| `show/` | 配信に持ち込むものの置き場。`slides/`、`scripts/`、`motions/`、`bgm/`。git 管理外でビルドにも含まれず、`--slides`・`--scripts`・`--motions`・`--bgm` で各ライブラリの場所を移せます。 |

## 効いている継ぎ目

**通信の語彙は 1 か所にしかありません**。`src/protocol` の zod スキーマです。コマンドを増やすとは、まずここに足すことで、ビューアもサーバも CLI もそれに従います。リリース周期の違う 3 プロセスがずれないのはこれのおかげです。

**エンジンは自分がブラウザにいることを知りません**。依存するのは three.js だけで、`AudioContext` も DOM も `fetch` も使いません。「喋られた台詞とは何か」を型で述べるだけで、実際の音は `AudioContext` を持つビューアが用意します。テストがエンジン全体を合成アバターに対してヘッドレスで走らせられるのはこのためです。

**常設状態はサーバが持ちます**。アバター、衣装、画角、背景、音の部屋、声の処理、BGM の再生状態、調律は、選ばれた時点でひとつの設定に畳み込まれ、レンダラが接続した瞬間に、積んである台詞ごと手渡されます。配信の内容がブラウザソースの URL に入っていないので、配信中の再読み込みに耐えます。[操作面](surfaces.md)を参照してください。

**MCP アダプタは判断を持ちません**。どのツールも上の HTTP エンドポイントそのもので、そこに、いま立っているアバターの id をスキーマとして埋め込んであるだけです。2 つめの制御面ではなく、言い換えです。[MCP アダプタ](mcp.md)を参照してください。

**音声サイドカーは直接呼ばず、中継します**。ビューアは `POST /api/speech` 経由で呼びます。呼び手が 1 つだからこそ、声はポートではなく UNIX ソケットで答えられます。ページはソケットを名指しできませんし、この機械の外からは届きません。ランタイムの他が bind をループバックに縛っているのと同じ規則を、より厳しく適用できる場所で適用したものです。直接呼べるように声をポートへ戻すのは、コードの変更である前にライセンスの判断です。

## 次に読むもの

- [制御 API](control-api.md)
- [アバター](avatars.md)
- [操作面](surfaces.md)
