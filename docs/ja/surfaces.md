# 操作面

ページは 3 つあります。違うのはできることではなく、立っている場所です。

## パネル（`/panel/`）

配信を回す場所です。レンダラも `AudioContext` も持たず、操作はすべて制御 API を通って出ていきます。ここで動く操作は、言語モデルにも動かせる操作です。

タブは 9 枚です。キュー（どの台本が入っているか、待機中の行、その履歴、途中への巻き戻し）、録画（ファイルへの書き出し）、演じる、音声、BGM、資料、装う、調律、診る。BGM タブでは `show/bgm/` の一覧、再生、音量、BGM 専用の libsonare エフェクトを扱います。アバター・画角・背景・アイドルの切替はプレビューの下にあります。この 4 つは絵を見て決めるものだからです。

絵の下には、いま喋っている行が文字で出ます。キュータブではなくここにあるのは、この読み出しがどのタブにも属するからです。1 つのセグメントは資料タブでページを送りながら過ぎていきますし、「いま何と言っているのか」の答えは一晩中同じ場所にある必要があります。中身によらず高さは 3 行分で固定なので、発話が始まっても下の操作がずれません。

プレビューがそのままカメラ操作です。ドラッグでキャラクターの周りを回り、ホイールで寄ります。埋め込まれたビューアが自分のカメラを読み返してパネルへ渡し、パネルはオーケストレータが送るのと同じ `camera` コマンドを送ります。本番のレンダラも一緒に回りますし、ショットの情報がどちらか一方のページだけに存在することもありません。

プレビューは常に無音です。同じコマンドを受け取る 2 つ目のレンダラなので、行はすべて要求しますし、音も最後まで再生します。口の動きが実際のキューと同じ時計で進むようにするためで、その音はゲイン 0 に流れ込みます。本番に出ている音とこちらの音を両方聞くと、1 行がコンマ何秒ずれて二重に聞こえます。

この無音が、どのページが録画するかも決めています。録画に入るべきものは部屋が聞いたものなので、書き出すのはミュートされていないほうのレンダラです。[録画](recording.md)を参照してください。

タブの上には、OBS に必要なアドレスが 1 本だけ並びます。手で打つのではなく、サイズ・背景・資料・キャラクターの立ち位置・背景の透過を選ぶと `/?size=…` の形で組み上がります。貼り付けた瞬間からその設定は OBS のものなので、アプリ側の状態としては持ちません。

## レンダラ（`/`）

OBS が向くページです。開くとキャラクターだけが出ます。コンソールも HUD もカーソルもなく、カメラはポインタで動きません。本番前に切らなければならない既定値は、いつか本番に乗る既定値だからです。

`?console=1` で操作卓が戻ります。こちらは動いているシーンの**中**に手を入れます。`director.rig.measure('R')`、解剖学的な範囲に対する関節角、解決済みのプロファイルを扱えます。2 つめの制御面ではなく開発用の計器で、ポーズが変に見える原因を調べるためのものです。（`?stage=1` も受け付けますが、いまは既定と同じで何も足しません。既定が変わる前に設定されたブラウザソースに書かれているので残してあります。）

## レンダラの URL

そのソースが何であるかは、すべてアドレスに載せます。ブラウザソースとは、OBS が任意のタイミングで読み直すテキストフィールドだからです。対して番組の内容はサーバの常設状態にあり、レンダラが接続した瞬間に手渡されます。

| パラメータ | 値の例 | 既定 | 何を決めるか |
|---|---|---|---|
| `size` | `1920x1080` | ウィンドウいっぱい | ピクセル単位の描画サイズ。OBS で設定したソースサイズに合わせないと、画が 2 回リサンプルされます |
| `backdrop` | 背景の id | 素の背景 | キャラクターが見えている部屋。[ステージ](stage.md#背景)を参照してください |
| `transparent` | `1` | オフ | 後ろに何も描かず、ゲームキャプチャの上への合成を OBS に任せます。背景を選べばそちらが勝ちます |
| `deck` | スライドの id | なし | そのソースが開いた時点で出すスライド。[スライド](slides.md)を参照してください |
| `place` | `bottom-right:0.32x0.6` | フレーム全体 | フレームのどこに立たせるか。基準の隅と、フレームに対する 2 つの比で指定します。[ステージ](stage.md#フレームのどこに立たせるか)を参照してください |
| `mute` | `1` | オフ | このレンダラが音を出すかどうか。次節を参照してください |
| `console` | `1` | オフ | 操作卓・HUD・カーソル。3 つで 1 つの判断なので、フラグも 1 つです |
| `debug` | `1` | オフ | 計測表示を、レンダラが**開いた時点で**出すかどうか。その後はバッククォートキーと `debug` コマンドで動かします |
| `stage` | `1` | — | 受け付けますが何もしません。既定と同じ内容です |

解釈できない値は、失敗ではなく劣化して動きます。知らない背景 id は素の背景に、壊れた `place` はフレーム全体に、大きすぎる `size` はウィンドウいっぱいになります。レンダラにはエラーの報告先がありません。URL は OBS の中のフィールドに打ち込まれたものだからです。そのため打ち間違いは、それでも配信が続く状態に落ちる必要があります。

このうち 5 つはパネルの仕事でもあります。タブの上に並ぶアドレスは `size`・`backdrop`・`deck`・`place`・`transparent` から組み立てられます。ブラウザソースの設定をパネルで行うのはそのためで、どこかから URL をコピーしてくる必要はありません。

`/monitor/` は自分のアドレスに付いたクエリ文字列を、内側のビューアへそのまま渡します。ここに挙げたものはステージ側でも同じように効きます。

### レンダラをミュートする

仕組みは `?mute=1` がすべてで、アドレスを変えずにレンダラを黙らせるコマンドは意図的に用意していません。静かなページは、アドレスを読んで理由の分かる形で静かでなければならないためです。

これが黙らせるのは**出力であって合成ではありません**。ミュートされたレンダラも、行はすべて要求しますし、音も最後まで再生します。ただし流れ込む先はゲイン 0 です。要求ごと省けば時計が別になり、口はテキストからの推定尺に戻り、行の終わる瞬間が本番とずれるため、監視対象より先に進むモニタになってしまいます。

このフラグ 1 つから、結果が 3 つ出てきます。

- パネルのプレビューはミュート付きで開きます。オペレータが 1 行を二重に聞かないためです。
- ステージウィンドウは `Window → Mute Stage` によって、付きでも無しでも開きます。[ネイティブシェル](shell.md#mute-stage)を参照してください。
- 録画を書き出すのは、ミュートされて**いない**ほうのレンダラです。[録画](recording.md)を参照してください。

## ステージ（`/monitor/`）

同じレンダラを 16:9 にレターボックスして、それ以外は何も置かないページです。コンソールも操作系も、囲うアプリケーションもありません。オペレータが見る画面であり、このプロジェクトで唯一、聞くためのページでもあります。OBS が開くブラウザソースの音は配信へ流れるので、ふつうの卓では部屋にいる人にキャラクターの声は届きません。

パネルのコードもキュー編集のコードも含みません。中身は `/` を指す iframe が 1 つと、その外側の黒帯だけです。帯を外に出してあるので、ウィンドウをどんな形に伸ばしてもレンダラはソースと同じアスペクト比を受け取ります。自分のアドレスに付いたものはそのまま素通しで、`?mute=1` も含みます。OBS 側でブラウザソースをモニタリングしていて部屋がすでに声を聞いている場合は、これで黙らせます。

## ネイティブシェル

`yarn shell` はページをビルドして、パネルとステージの 2 枚のウィンドウを開きます。制御サーバと、チェックアウトにあれば音声サイドカーもその下で起動し、終了時に一緒に落とします。同じチェックアウトのサーバがすでに動いていれば、置き換えずにそれを使います。立ち上げっぱなしの `yarn dev` を落とさないためです。**別の**チェックアウトのサーバだった場合は、黙って使わずに名指しで拒否します。他人のビルドから読み込まれた 2 枚のウィンドウは、レンダラが立ち上がらない状態と見分けがつかないためです。

これは 4 つ目の操作面ではなく、便宜のためのものです。中で開いているのは同じループバックアドレスの同じ 3 ページで、配信に対してできることも同じ制御 API を通ります。足しているのはブラウザにできないことだけです。ウィンドウは前回置いた場所に戻り、ステージはページをクリックしなくても音を出しはじめ、`Window → Mute Stage` でこの機械が声を聞くかどうかを決められます。配信について、オーケストレータにも台本の 1 行にも答えられない唯一の問いです。番組の話ではなく、卓の話だからです。

メニューの中身、状態表示、ログの置き場所、ウィンドウに何を許しているかは[ネイティブシェル](shell.md)にまとめてあります。

## 計測の表示

`?debug=1` はレンダラが描くもう 1 つのもので、本番の画面に出しうる唯一のものです。呼吸・瞬き・視線・フレームレート、それにどのスライドが出ていてそのページが描き終わっているかを、シェル風にフレームの隅へ出します。スライドの層より上に置いてあるので、スライドの区間でも隠れません。読むだけなので、本番中に出しても害がありません。バッククォートキーで切り替えます。実際の使い方もこちらで、疑問が出たときに出して、数秒後に消します。

コマンドでもあります。知りたいのはたいてい目の前のページではなく、配信に出ている画面のことだからです。

```sh
yarn ctl debug          # 接続中のすべてのレンダラに出す
yarn ctl debug off
```

パネルではプレビューの上の measure ボタンです。プレビューにも出るのは、プレビューが同じコマンドを受け取っている 2 つ目のレンダラだからです。

**常設に見えて、意図的に保存しない唯一の設定です**。発話をまたいで残る設定、つまりアバター、衣装、画角、背景、音の部屋はすべて、レンダラが接続した瞬間に手渡される設定に畳み込まれます。これだけは意図的に外してあります。そうしないと、リハーサル中に出した計測値が、配信の頭で OBS がソースを読み直したときに独りでに戻ってきます。新しく接続したレンダラは、直前に何が送られていようと必ず消えた状態から始まります。

## 常設状態

パネルが操作面の全部で、レンダラは配信の頭に最後に開きます。そうできるのは、制御サーバが設定を保持しているためです。アバター、衣装、画角、背景、音の部屋、声の処理、BGM の再生状態、調律は、選ばれた時点で常設状態に畳み込まれ、レンダラが接続した瞬間に、積んである台詞ごと手渡されます。配信中の再読み込みに耐えられるのはこのためです。

配信の内容が、ブラウザソースに設定した URL に入っていることはありません。

再現しないのは、設定ではなく一瞬だったものです。ジェスチャは自然に終わり、表情はそれを上げた行とともに解除され、割り込みはすでに起きています。1 時間後に接続したレンダラのためにそれを演じ直すのは、設定の復元とは逆の動作になります。

## 次に読むもの

- [ネイティブシェル](shell.md) — このうち 2 ページをウィンドウで開くもの
- [録画](recording.md) — このうちどのページがファイルを書くか
- [構成](architecture.md) — 常設状態の置き場所
- [制御 API](control-api.md) — パネルが送っているもの
